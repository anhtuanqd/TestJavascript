const resize = () => {
  let arr = [];
  setTimeout(()=> {
    // let tableHover = document.querySelectorAll('.tableHover')
    // arr = Array.from(tableHover)
    // arr.map(item => {
    //   console.log(item.getBoundingClientRect());
    // })
  },2000)
}
export default resize;
